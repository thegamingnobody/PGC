<Window x:Class="PGC.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PGC"
        xmlns:vm="clr-namespace:PGC.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">


    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <Grid>
        <ContentControl x:Name="match">
            <Border Background="#FF2D2D30"
                    Margin="10">
                <StackPanel>
                    <TextBox Margin="10" Text="{Binding NewPlayerName}"/>
                    <Button Content="Add Player"
                            Command="{Binding AddPlayerCommand}"
                            Margin="10" />
                    <ListBox ItemsSource="{Binding Path=Players, UpdateSourceTrigger=PropertyChanged}">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}"
                                               Margin="5" />
                                    <TextBlock Text="{Binding Score}"
                                               Margin="5" />
                                    <Button Content="Remove"
                                            Command="{Binding ElementName=match, Path=DataContext.RemovePlayerCommand }"
                                            CommandParameter="{Binding}"
                                            Margin="10" />
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>

            </Border>
        </ContentControl>
    </Grid>
</Window>
